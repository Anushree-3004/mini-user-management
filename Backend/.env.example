# Backend environment variables (example)

# Required for local dev
MONGODB_URI=mongodb://127.0.0.1:27017/delicious_sweets

# Used to sign JWTs
JWT_SECRET=change_me_in_real_env

# Registering with this email will create an admin user
ADMIN_EMAIL=admin@example.com

# Server port
PORT=5000
